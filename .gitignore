# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Secrets — provider credentials live here, never committed
.env

# Editable install / venv
.venv/
venv/

# Tooling caches (local code-intelligence + editor rules)
.codegraph/
.cursor/

# Lean / lake build artifacts (the kernel builds inside the container)
.lake/
lean-project/.lake/

# OS
.DS_Store
