# build output
dist/
.astro/

# deps
node_modules/

# generated content — written from ledger/calculemus.json by sync-ledger.mjs.
# The ledger itself IS committed (the published ledger is public); the derived
# collections are not.
src/content/laws/
src/content/cycles/
src/content/sync-report.json

# env / secrets
.env
.env.*
!.env.example

# os
.DS_Store
